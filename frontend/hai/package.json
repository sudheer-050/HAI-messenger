{
  "name": "hai-quick-actions-parser",
  "private": true,
  "type": "module",
  "description": "Standalone deterministic Quick Actions (HAI) text-command parser. No runtime dependencies.",
  "scripts": {
    "test": "node --test"
  }
}
